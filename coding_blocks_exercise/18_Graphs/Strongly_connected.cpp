// no of components where each member can visit all nodes in that component

// 1) do a dfs over gaph storer oder (push such that from that node we cant go anywherre)

// 2) reverrse graph

// 3) do a dfs again frrom left to right put a ptr (every time we have to use the loop to enter a node incrase ptr)

// ans is ptr