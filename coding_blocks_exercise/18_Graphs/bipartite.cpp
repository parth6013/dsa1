// biparite graph such a graph where nodes are divided into two sets
// where edges arre from set to another

// or biparite means 2 colourr problem  where imeediate neighbours are not of same color

// How to check 
// 1) if graph has a cycle and no of nodes = odd
// 2)or if colour of next visited node is same as this one